#!/usr/bin/env bash

CPUS="0 1 2 3"
MAX_FREQ="3.4Ghz"

FREQ=${1:-${MAX_FREQ}}

for CPU in $CPUS; do
    sudo cpufreq-set -c ${CPU} --max ${FREQ}
done

for CPU in $CPUS; do
    cpufreq-info -c ${CPU} -p
done
