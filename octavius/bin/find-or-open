#!/usr/bin/env bash

CLASS="${1}"
TITLE="${2}"
LAUNCH_CMD="${3}"

WINDOW_LIST=$(wmctrl -lx | awk '{printf $1"\t"$3"\t"; for (i=5;i<=NF;i++) printf $i" "; print ""}')
MATCH=$(awk -F"\t" "{ if ( (\$2 == \"${CLASS}\") && (\$3 ~ /${TITLE}/) ) { print }}" <<< "${WINDOW_LIST} }")


echo "${MATCH}"
