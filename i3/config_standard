# i3 Config File
# Created: 2020-01-21
# As much as possible, make it act like emacs manual tiling

# Settings

# Font
font pango:monospace 8
#font pango:DejaVu Sans Mono 8

## All workspaces start in tabbed layout
workspace_layout tabbed

## Modifier for floating window manipulation
floating_modifier Mod4

## Colours
# class                 border  bground text    indicator child_border
client.focused          #006600 #004400 #FFFFFF #004400   #006600
client.focused_inactive #333333 #222222 #FFFFFF #FF0000   #333333
client.unfocused        #333333 #555555 #FFFFFF #FF0000   #333333
client.urgent           #FF0000 #FF0000 #FFFFFF #FF0000   #FF0000
client.placeholder      #FF0000 #FF0000 #FFFFFF #FF0000   #FF0000

client.background       #FFFFFF

## Keybind shortcuts

set $i3c ~/.config/i3/i3c
set $exit mode "default"

### default
set $mod Mod4

### Meta + Hyper
set $s1 Mod1+Mod4

### Control + Shift + Hyper
set $s2 Control+Shift+Mod4

### Control + Meta + Hyper
set $s3 Control+Mod1+Mod4

### Shift + Meta + Hyper
set $s4 Shift+Mod1+Mod4

# Key tables

## Windows
mode "window" {
    bindsym h exec $i3c split-right ; $exit
    bindsym v exec $i3c split-down ; $exit
    bindsym u focus parent ; $exit
    bindsym d focus child ; $exit
    bindsym m fullscreen toggle ; $exit
    bindsym f floating toggle ; $exit

    bindsym Escape $exit
}

mode "frame" {
    bindsym r exec $i3c rename-workspace $(echo -n | dmenu -p "Rename workspace:") ; $exit
    bindsym c exec $i3c create-new-workspace ; $exit

    bindsym Escape $exit
}

mode "buffer" {
    bindsym y exec $i3c buffer-yank ; $exit
    bindsym p exec $i3c buffer-paste ; $exit
    bindsym j exec $i3c jump-to-window ; $exit
    bindsym b exec $i3c bring-window ; $exit

    bindsym Escape $exit
}

# Mouse bindings

## The middle button over a titlebar kills the window
bindsym --release button2 kill

# Keybindings

bindsym Mod4+Shift+l exec i3lock -fc 222222
bindsym Mod4+space exec i3-sensible-terminal

## SYS-1

bindsym $Mod4+1 exec $i3c focus-workspace 1
bindsym $Mod4+2 exec $i3c focus-workspace 2
bindsym $Mod4+3 exec $i3c focus-workspace 3
bindsym $Mod4+4 exec $i3c focus-workspace 4
bindsym $Mod4+5 exec $i3c focus-workspace 5
bindsym $Mod4+6 exec $i3c focus-workspace 6
bindsym $Mod4+7 exec $i3c focus-workspace 7
bindsym $Mod4+8 exec $i3c focus-workspace 8
bindsym $Mod4+9 exec $i3c focus-workspace 9
bindsym $Mod4+0 exec $i3c focus-workspace 10

bindsym $Mod4+h exec $i3c focus-left
bindsym $Mod4+j exec $i3c focus-down
bindsym $Mod4+k exec $i3c focus-up
bindsym $Mod4+l exec $i3c focus-right

bindsym $Mod4+q kill
bindsym $Mod4+b mode "buffer"
bindsym $Mod4+o exec dmenu_run
bindsym $Mod4+w mode "window"
bindsym $Mod4+comma exec $i3c prev-tab
bindsym $Mod4+period exec $i3c next-tab

## SYS-2

bindsym $Mod4+Mod1+h resize shrink width 2 px or 2 ppt
bindsym $Mod4+Mod1+j resize grow height 2 px or 2 ppt
bindsym $Mod4+Mod1+k resize shrink height 2 px or 2 ppt
bindsym $Mod4+Mod1+l resize grow width 2 px or 2 ppt

## SYS-3

bindsym $Mod4+Shift+1 move container to workspace 1
bindsym $Mod4+Shift+2 move container to workspace 2
bindsym $Mod4+Shift+3 move container to workspace 3
bindsym $Mod4+Shift+4 move container to workspace 4
bindsym $Mod4+Shift+5 move container to workspace 5
bindsym $Mod4+Shift+6 move container to workspace 6
bindsym $Mod4+Shift+7 move container to workspace 7
bindsym $Mod4+Shift+8 move container to workspace 8
bindsym $Mod4+Shift+9 move container to workspace 9
bindsym $Mod4+Shift+0 move container to workspace 10

bindsym $Mod4+Shift+f mode "frame"

bindsym $Mod4+Shift+h move left
bindsym $Mod4+Shift+j move down
bindsym $Mod4+Shift+k move up
bindsym $Mod4+Shift+l move right

bindsym $Mod4+Shift+p exec $i3c prev-workspace
bindsym $Mod4+Shift+n exec $i3c next-workspace
bindsym $Mod4+Shift+comma exec $i3c swap-workspace-left
bindsym $Mod4+Shift+period exec $i3c swap-workspace-right

## SYS-4

bindsym $Mod4+Control+n scratchpad show
bindsym $Mod4+Control+m move scratchpad
bindsym $Mod4+Control+c floating toggle

# toggle tiling / floating
# bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

exec_always --no-startup-id ~/.config/i3status/run_dzen2_bar